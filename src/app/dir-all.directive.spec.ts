import { DirAllDirective } from './dir-all.directive';

describe('DirAllDirective', () => {
  it('should create an instance', () => {
    const directive = new DirAllDirective();
    expect(directive).toBeTruthy();
  });
});
